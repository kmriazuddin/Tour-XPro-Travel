import SectionTitle from "../../Shared/SectionTitle/SectionTitle";

const AddReview = () => {
    return (
        <div>
            <SectionTitle heading={'Add Review Section'}></SectionTitle>
        </div>
    );
};

export default AddReview;